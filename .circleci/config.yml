version: 2.1

orbs:
  aws-s3: circleci/aws-s3@2.0.0

jobs:
  build-and-deploy-wiki:
    docker:
      - image: cimg/go:1.18
    steps:
      - checkout
      - run:
          name: 🍺 Installing Bock
          command: |
            wget https://public.nikhil.io/bock-linux
            chmod +x bock-linux
      - run:
          name: 📓 Building Wiki
          command: |
            ./bock-linux -a . -o /home/circleci/output
      # - run:
      #     name: 🗜 Compressing
      #     command: |
      #       pushd /home/circleci/output
      #       find . -type f -exec gzip -9 '{}' \; -exec mv '{}.gz' '{}' \;
      #       popd
      - aws-s3/sync:
          aws-region: AWS_DEFAULT_REGION
          aws-access-key-id: AWS_ACCESS_KEY_ID
          aws-secret-access-key: AWS_SECRET_ACCESS_KEY
          from: /home/circleci/output
          to: "s3://docker.nikhil.io/"
          # arguments: |
          #   --delete \
          #   --size-only \
          #   --content-encoding gzip

workflows:
  version: 2
  main:
    jobs:
      - build-and-deploy-wiki:
          # Don't care about running the above for anything other than pushes
          # to the master branch 🤷‍♂️
          filters:
            branches:
              only: master